;;; helm-everywhere -- Contains customizations specific to using Emacs in OS X

;;; Commentary:

;;; Code:

(global-set-key (kbd "C-x C-f") 'ido-find-file)

(provide 'helm-everywhere)
;;; helm-everywhere.el ends here
